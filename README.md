# Installation

Required Dependencies:

```
pip install numpy matlibplot scipy nibabel pandas scikit-learn networkx infomap

git submodule update --init --recursive
cd ENIGMA
python setup.py install
```