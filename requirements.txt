# Core scientific stack
numpy
pandas
scipy
matplotlib

# Neuroimaging
nibabel
nilearn
vtk==9.2.2  # lowest version available for Python 3.10; ENIGMA needs patching for 9.1+

# Graph/network analysis
networkx
infomap
leidenalg
python-igraph
ndlib                 # Network Diffusion Library — cascade/threshold models

# Machine learning
scikit-learn

# Jupyter / tooling
jupyter
ipykernel
tqdm
Pillow                # required for matplotlib GIF output (PillowWriter)
jupytext              # convert .ipynb ↔ .py

# VTK offline rendering (WSL/headless via OSMesa)
# itkwidgets and trame are optional — only needed for interactive Jupyter VTK widgets
# itkwidgets
# trame
# trame-vtk

# ENIGMA Toolbox – install from submodule:
#   git submodule update --init --recursive
#   cd ENIGMA && python setup.py install
